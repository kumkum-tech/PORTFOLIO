Add these files here to make the site fully functional:

- profile.jpg   -> your headshot / profile photo (not currently used in the layout, but reserved here per the project structure)
- resume.pdf    -> your resume file. Once added, update the three resume buttons in index.html
                   (search for "resume-btn") to link directly to "public/resume.pdf" instead of
                   showing the placeholder alert.
